# Base URL of the running GraderPlus backend (NestJS), no trailing slash.
# Confirm the actual port with Dayshin — backend front-end-back-end-guide.md
# does not document one; Nest's default is 3000.
VITE_API_BASE_URL=https://api-graderplus.duckdns.org/
