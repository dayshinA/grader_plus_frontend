import { ProjectWorkspacePage } from "~/features/grading/components/project-workspace-page";

export function meta() {
  return [{ title: "Marking — GraderPlus" }];
}

export default function ProjectDetail() {
  return <ProjectWorkspacePage />;
}
