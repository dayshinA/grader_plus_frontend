/**
 * Audit is built and working but hidden from the app for now: the sidebar entry, the two
 * tabs, the home screen shortcut and the three screens themselves.
 *
 * Nothing has been deleted. The feature folder, the service, the hooks, the table and the
 * three route modules are all still here and still compile, and the routes stay registered
 * so the paths do not change when this comes back. Set this to false and every entry point
 * returns.
 */
export const AUDIT_HIDDEN = true;
